<%= form_for(@merchant_marketing_rule) do |f| %>
  <% if @merchant_marketing_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@merchant_marketing_rule.errors.count, 'error') %> prohibited this merchant_marketing_rule from being saved:</h2>

      <ul>
      <% @merchant_marketing_rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
